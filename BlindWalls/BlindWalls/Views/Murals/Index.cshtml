@model IEnumerable<Domain.Entities.Mural>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MuralName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MuralDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ArtistID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MuralName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MuralDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ArtistID)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditMural", new { id=item.MuralId }) |
            @Html.ActionLink("Detail", "MuralDetail", new { id=item.MuralId }) |
            @Html.ActionLink("Delete", "DeleteMural", new { id=item.MuralId })
        </td>
    </tr>
}

</table>

<p>
	@Html.ActionLink("Create New", "Create")
</p>
